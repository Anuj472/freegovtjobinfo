# Cloudflare Pages redirect rules
# NOTE: Static files (xml, txt, assets) are automatically served if they exist
# Only non-existent routes fallback to index.html

# SPA fallback - serve index.html for all non-file requests
/* /index.html 200